import React from "react";

const GradientCode = ({
    colorStart,
    colorEnd
}) => {
    return (
<code > backgrond - image: linear - gradient(to right, 
    {colorStart}, {colorEnd
        }); </code>
    );
};

export default GradientCode;